2026-03-21 架构评审摘要：本仓库核心链路是 filter -> algorithm -> policies -> optional objective -> fallback，入口在 balancer/balancer.go:66(New) 与 235(Route)。已核验 go test ./... 通过，工作区干净。与外部项目对比后，重点风险/改进点：1) balancer.go:285-286 注释仍声称过滤模型可用性，但模型语义路由已在此前变更中移除，形成注释/实现不一致；2) plugin/algorithm/plugin.go 与 plugin/policy/plugin.go 接口都不接收 context，只有 objective.ContextPlugin 支持 ctx，限制未来做超时、远端状态查询、预算检查、共享状态访问；3) registry/registry.go 使用全局默认注册中心与无参工厂 func() Plugin，插件缺少 typed config 注入能力；4) types.NodeSnapshot 只有 Healthy/Outlier/FreshnessTTLms 等轻量字段，缺少状态时间戳/版本/来源契约，stale 语义偏弱；5) sessionAffinity 为 balancer 进程内 map，无 TTL/容量限制/跨实例共享；6) fallback 仅支持 policy_ranked 或算法名，缺少按原因分类的降级矩阵；7) reliability pilot 的 outlier 处理是 soft degrade 语义，所有节点均 outlier 时回退到原集合，缺少 panic threshold/min healthy ratio 等显式策略。对标样本：Envoy（cluster/health_checks/outlier_detection/load_balancing_policy 分层），Traefik（provider->router/middleware/service->loadbalancer+healthcheck），Fabio（Consul watch -> route.NewTable/SetTable 原子更新），LiteLLM（Router 管理 routing_strategy/fallbacks/retries/cooldowns/Redis 共享状态），AIBrix（gateway algorithms + prefix_cache + podautoscaler 控制环）。